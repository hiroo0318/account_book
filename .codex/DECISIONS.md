# Decision Record

| Date | Decision | Rationale | Rejected alternatives | Impact |
| --- | --- | --- | --- | --- |
| 2026-09-20 | Build a monthly-only food expense ledger. | The product is intended to manage food-ingredient purchases and meals as one combined spending area. | A full household ledger; separate purchase and meal categories. | The entry model has no category field. |
| 2026-09-20 | Store only month, item name, and KRW amount per entry. | The user explicitly does not need transaction dates or classifications. | Daily records and category analytics. | The core table and form remain deliberately small. |
| 2026-09-20 | Use Supabase Auth with email/password and a user-owned RLS table. | Entries must synchronize across devices while remaining private to each account. | Device-only storage; anonymous access. | Requires secure authentication flows and ownership policies. |
| 2026-09-20 | Support edit, delete, and adjacent-month navigation. | Corrections are necessary for dependable household use, and month-by-month movement is the requested simple navigation model. | Append-only records; calendar picker; month tab list. | The mobile UI needs editable rows, deletion confirmation, and previous/next controls. |
| 2026-09-20 | Open an entry detail/edit layer when a list row is tapped. | This keeps the monthly list focused and avoids crowded per-row controls on a small screen. | Always-visible edit and delete buttons on every row. | The layer shows the item and amount again, with save and delete actions. |
| 2026-09-20 | Add entries from a `+ 추가` button within the monthly summary card. | Addition remains accessible without a permanent form or a detached header control. | An always-visible inline add form; a bottom add button; a standalone header button. | The same layer supports creation and editing, with actions appropriate to each mode. |
| 2026-09-20 | Make the selected month large and centered, with small adjacent month arrows. | The selected month is the screen's primary navigation context; changing it is secondary. | Equal-size month controls; a calendar picker. | The header uses a strong centered month label with visually quiet arrow buttons. |
| 2026-09-20 | Present the monthly total in one emphasized card beneath the month header. | The monthly total is the primary result the user needs at a glance. | Unframed text total; multiple summary metrics. | The card has a small label and a large formatted KRW amount. |
| 2026-09-20 | Use a navy and cobalt-blue visual palette. | It gives the tracker a clean, contemporary character without the earlier all-green tone. | Olive/green palette. | The summary card, primary action, text accents, and pale background use cool blue tones. |
| 2026-09-20 | Use Google OAuth with an approval-pending membership flow. | The shared family ledger requires access control, not separate personal ledgers. | Anonymous sessions; self-approved membership; email/password UI. | First Google sign-in creates a pending member; only an administrator can promote the two intended accounts to active. |
| 2026-09-20 | Store one shared ledger in `account book.expense_entries` and expose security-invoker public views. | The requested custom schema remains the source of truth while the React client uses the default exposed API schema. | Per-user expense tables; exposing the custom schema to the Data API. | RLS permits ledger operations only when the caller has an active member row. |
| 2026-09-20 | Move the feature to Supabase project `ultrjuxpudxlfllovcqq`. | The user selected this project as the correct destination. | Continuing on `lnlghfazjjzijejjltjl`. | The React environment file and all new account-book tables point to the new project. |
| 2026-09-20 | Limit account management to logout and password reset in v1. | Provides basic account safety without expanding the initial service scope. | Email change and account deletion. | No profile-management or data-erasure workflow is planned. |
